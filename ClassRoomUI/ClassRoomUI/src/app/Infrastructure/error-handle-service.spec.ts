import { ErrorHandleService } from './error-handle-service';

describe('ErrorHandleService', () => {
  it('should create an instance', () => {
    expect(new ErrorHandleService()).toBeTruthy();
  });
});
